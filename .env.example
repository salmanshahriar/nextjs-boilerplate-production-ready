# App URL (optional). Overrides domain from app/seo/app-main-meta-data.json for sitemap, robots, manifest.
# Leave empty to use domain from SEO config.
# NEXT_PUBLIC_APP_URL=https://yourdomain.com

# Uncomment when you add a real auth backend:
# AUTH_API_URL=https://api.yourdomain.com
# NEXT_PUBLIC_AUTH_ENABLED=true
